/**
 * 변수를 상수화 시키는 방법
 * const를 사용해 변수를 상수화 시켜 변경 가능
 * ex) const int a = 5;    ->     a 값은 5 로 고정되어 초기값을 변경 불가능함
*/

/**
 * 자동 형 변환
 * 예를 들어 , 10.0 + 1나 10.0/4의 결괏값이 float일지 integer일지?
 * 보통 자료형의 크기가 큰 쪽으로 통일시킨다. 위의 예제의 경우 double이 integer보다 크기 때문에 
   (8byte vs 4byte) double로 통일된다. 
 * 자료형의 크기는 char < int < long < float < double이다.
*/
