/**
 * literal
 * 변하지 않는 상수
 * 정수형 literal / 부동소수점형 literal
 * 문자형(char) literal - 1바이트 문자 , 한글은 한 글자당 2바이트를 차지해 literal 불가능함
 *  - 문자형 literal은 양쪽에 ''로 감싸야 함
 * 문자열(string) literal - 두개 이상의 문자 묶음, 한글 한 단어는 string으로 묶일 수 있음
 * - 문자열 literal은 양쪽에 ""로 감싸야 함
*/

/**
 * 식별자(identifier)
 * 변수 , 함수 등에 부여하는 이름으로 , 따로 이름을 붙여 사용 가능하다.
 * 변수나 함수의 이름을 정할 때 한글 , 예약어 등은 사용하지 않는다.
 * 상수를 사용할 때에는 그냥 사용이 가능하지만 literal은 ''를 사용한다.
*/
